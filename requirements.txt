# Runtime dependencies, mirroring [project.dependencies] in pyproject.toml.
# Prefer `pip install measureia` or `uv sync`; this file is for installing the
# dependencies by hand. For development, add pytest (see [dependency-groups]).
astropy>=6.1; python_version < '3.11'
astropy>=7.0; python_version >= '3.11'
h5py>=3.11
matplotlib>=3.9
numpy>=2.1
pyccl>=3.3.4
scipy>=1.15
sympy>=1.12
